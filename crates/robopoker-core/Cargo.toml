[package]
name = "robopoker-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[features]
default = ["std"]
std = []
shortdeck = []

[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive"] }

[dev-dependencies]
rand = { version = "0.9.1", features = ["small_rng"] }
